# Bản GPU (NVIDIA). Cài SAU khi đã có requirements.txt, rồi THAY 2 gói CPU bằng GPU:
#
# 1) onnxruntime GPU (cho OCR):
#      pip uninstall -y onnxruntime
#      pip install onnxruntime-gpu
#
# 2) torch CUDA (cho NSFW) — chọn kênh khớp driver (nvidia-smi -> CUDA Version):
#      pip uninstall -y torch
#      pip install torch --index-url https://download.pytorch.org/whl/cu128   # CUDA 12.8
#      # hoặc cu126 / cu121 tuỳ driver
#
# QR/WeChat vẫn CPU (opencv-contrib-python pip không có CUDA) -> không cần đổi.
#
# Kiểm tra:
#   python -c "import torch; print(torch.cuda.is_available(), torch.cuda.get_device_name(0))"
#   python -c "import onnxruntime as o; print(o.get_available_providers())"  # phải có CUDAExecutionProvider
#
# Chạy:  python moderate.py sample --device gpu
